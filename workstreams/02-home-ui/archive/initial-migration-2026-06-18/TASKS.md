# 02-home-ui TASKS

## P1-2 整改（2026-06-18）

- [x] 阅读 `07-quality-review/HANDOFF.md` 并确认 P1-2 验收要求。
- [x] 为首页登录、搜索框、最新资讯“更多”、热门服务“全部”补充点击绑定。
- [x] 为四处未开放功能提供与操作对象对应的本地 toast 反馈。
- [x] 同步 `index.js` 与 `index.ts`，执行 JS 语法与镜像检查。

本轮仅修复首页误导性交互外观，不实现真实登录、搜索、资讯列表或服务列表。

## 七页入口集成（2026-06-18）

- [x] 阅读 05、08、09、10、11、12、13 workstream 交接并确认七个普通页面路由。
- [x] 启用企业招人、技能培训、看政策、校园招、用工信息、返乡专区、培训报名入口。
- [x] 为普通页面入口增加 `wx.navigateTo`，并保留找工作入口的 `wx.switchTab`。
- [x] 同步首页 JS/TS 数据和点击逻辑。
- [x] 执行入口映射、JS/TS 同步及 JS 语法检查。

七个已交付功能页现已从首页启用，统一使用 `type: "page"` 和 `enabled: true`；仅入口缺失、禁用或路由不完整时继续提示“功能建设中”。

- [x] 检查 `01-project-shell` 基础工程是否完成。
- [x] 等待 `01-project-shell` 创建可运行的小程序骨架。
- [x] 创建首页 mock 数据。
- [x] 实现顶部品牌区和搜索框。
- [x] 实现 8 个功能入口。
- [x] 实现最新资讯区。
- [x] 实现热门服务卡片。
- [x] 检查移动端视觉和文字溢出。
- [x] 更新 `HANDOFF.md` 说明完成内容。
- [x] 微调登录入口样式，去掉原生按钮输入框感。
- [x] 产出首页 8 个入口 UI 改版设计方案。
- [x] 主控线程和用户确认入口 UI 改版方案。
- [x] 将 8 个入口从 4 列小图标改为 2 列大字图章式入口。
- [x] 扩展首页入口 mock 数据字段，预留后续 API 和跳转。
- [x] 增加入口点击处理：找工作跳转职位 Tab，其他入口提示功能建设中。
- [x] 同步更新 `.js` 和 `.ts` 数据与页面逻辑。
- [x] 修复首页入口改版文件中的中文编码和字符串语法问题。
- [x] 执行 `node --check miniprogram/data/home.js` 和 `node --check miniprogram/pages/home/index.js`。
- [x] 更新 `HANDOFF.md` 说明本次入口 UI 改版实现、验收方式和后续 API 替换点。

## 完成说明

已基于 `miniprogram/pages/home/index` 实现首页静态 UI，并将首页展示内容集中放入 `miniprogram/data/home.ts` 与 `miniprogram/data/home.js`。当前不接真实后端，不实现真实搜索、登录、投递或聊天。

已新增 `ENTRY_UI_PROPOSAL.md`，并按确认后的“2 列大字图章式入口”方案完成首页入口区改版。当前仅 `找工作` 入口可通过 `wx.switchTab` 跳转职位 Tab，其他入口保持显示并提示“功能建设中”。
